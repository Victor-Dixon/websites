id: maskzero_home_migration_001
title: Move Mask Zero project from temp domain to canonical home
status: active
priority: P0
owner: Victor

domains:
  temp_project_domain: dadudekc.site
  canonical_project_domain: maskzero.site

rules:
  redirect_dadudekc_to_maskzero: false
  preserve_dadudekc_as_separate_site: true
  destructive_actions: false

objectives:
  - Replace Mask Zero project canonical references with maskzero.site.
  - Keep dadudekc.site as its own standalone site instead of a cross-domain redirect shell.
  - Keep MaskZero canonical on maskzero.site without breaking dadudekc.site ownership.
  - Deploy current Mask Zero project to maskzero.site.
  - Verify build and public routing.

verification:
  - Repo search confirms Mask Zero canonical URL is maskzero.site.
  - maskzero.site canonical configuration and public content do not depend on dadudekc.site redirects.
  - Build passes.
  - maskzero.site serves the current project.
  - dadudekc.site serves its own standalone site and does not redirect to maskzero.site.

report:
  path: data/reports/domain_migration/maskzero_home_migration_20260612_082218.txt
