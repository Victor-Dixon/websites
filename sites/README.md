## `sites/` (site configs)

This folder contains **site routing/config YAMLs** for the autoblogger pipeline.

Generated overlays/snippets now live under `websites/<domain>/overlays/`.

- Canonical site navigation is under `websites/` (see `websites/README.md`)
- Canonical site list is `config/sites_registry.json`
