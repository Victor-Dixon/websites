# Example environment variables for connecting to WordPress sites
#
# Copy to `.env` in repo root and fill in values.
# Never commit the real `.env`.
#
# ------------------------------------------------------------
# ariajet.site (per-site overrides)
# ------------------------------------------------------------
#
# WordPress REST API (recommended for posting content)
# Create an "Application Password" in WP Admin:
# Users -> Profile -> Application Passwords
ARIAJET_SITE_WP_SITE_URL=https://ariajet.site
ARIAJET_SITE_WP_USERNAME=your-wp-username
ARIAJET_SITE_WP_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx
#
# SFTP/SSH (used by deployment tools that upload theme/files)
ARIAJET_SITE_SFTP_HOST=your-sftp-hostname-or-ip
ARIAJET_SITE_SFTP_USER=your-sftp-username
ARIAJET_SITE_SFTP_PASS=your-sftp-password
ARIAJET_SITE_SFTP_PORT=65002
#
# ------------------------------------------------------------
# Generic fallbacks (optional)
# ------------------------------------------------------------
#
# If you set these, tools will use them when no site-specific vars exist.
WP_SITE_URL=
WP_USERNAME=
WP_APP_PASSWORD=
SFTP_HOST=
SFTP_USER=
SFTP_PASS=
SFTP_PORT=

