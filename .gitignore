# Local/runtime noise
__pycache__/
*.py[cod]
.DS_Store
*.tmp
*.log
.cache/

# Keep deployable zips tracked for now as release artifacts.
# If repo size grows, move zips to GitHub Releases and ignore:
# _hostinger_build/dist/*.zip

# Never deploy or treat source_review as runtime; still tracked as salvage until final cleanup.

# Raw legacy website consolidation folders.
# Local salvage only; canonical promoted assets live in _shared_plugins/ and _hostinger_build/.
TradingRobotPlugWeb/
FreerideinvestorWebsite/
DaDudeKC-Website/

# Dream.OS websites generated report governance
# Runtime reports and backup dumps are ignored by default; release manifests are committed intentionally.
_reports/*.tmp
_reports/**/*.tmp
_reports/**/*.log
_reports/**/*.json
_reports/backups/
_reports/**/backups/
!_reports/*manifest*.json
!_reports/*package*.json
!_reports/*release*.json
_hostinger_build/**/*.tmp
_hostinger_build/**/*.log
runtime/logs/
runtime/reports/**/*.json

# Remaining generated runtime artifact coverage
_reports/*.txt
_reports/**/*.txt

# Discord Architect generated runtime payloads
runtime/trading/discord/latest_paper_trade_payload.json
runtime/trading/discord/latest_cpc_closeout_bridge_payload.json
runtime/logs/discord_architect/
runtime/env/
data/exports/
